# Design System: منصة إدارة المطاعم والمحلات (DZ SaaS)

> المصدر الوحيد للحقيقة البصرية. أي شاشة جديدة (Stitch أو كود) يجب أن تلتزم بهذا الملف.
> القراءة: منصة تشغيلية B2B (لوحة + POS لمسي) للمالك/الكاشير/الطباخ الجزائري، بلغة هادئة موثوقة، على Tailwind v4 + shadcn/ui.
> الدالات: DESIGN_VARIANCE 4 / MOTION_INTENSITY 3 / VISUAL_DENSITY 7 (لوحة يومية كثيفة لكن مقروءة، وPOS عالي التكرار بلا حركة).

## 1. Visual Theme & Atmosphere

- **المزاج:** هادئ، عملي، موثوق. لا مهرجان ألوان ولا زجاجيات. السطح فاتح محايد مائل للرمادي المخضر، والحبر قريب من الأسود الدافئ.
- **الكثافة:** Daily-App كثيفة (7): تباعد قياسي `py-10/py-14` للأقسام، بطاقات مضغوطة في POS والمطبخ، وهواء سخي فقط حول رقم الفائدة اليومية.
- **التباين:** توزيع غير متماثل خفيف (4): شريط جانبي ثابت + محتوى، وبطل اللوحة يسار/يمين (رقم كبير + رسم)، لا تمركز كسول.
- **الحركة:** مقيدة (3): لا حركة في POS والأزرار عالية التكرار. دخول اللوحة فقط `opacity/translateY(6px)` متدرج 40ms، وخروج ألطف من الدخول. كل شيء ينهار إلى `opacity` فقط تحت `prefers-reduced-motion`.

## 2. Color Palette & Roles

لون مميز واحد فقط (النمو/الربح). الأحمر للخطر فقط، لا للزينة.

| الاسم | القيمة | الدور |
|---|---|---|
| **Canvas Mist** | `oklch(0.982 0.004 250)` / `#F6F8F7` | خلفية الصفحة |
| **Pure Surface** | `oklch(1 0 0)` / `#FFFFFF` | البطاقات والأسطح |
| **Charcoal Ink** | `oklch(0.235 0.018 265)` / `#1D2129` | النص الأساسي (لا pure black أبداً) |
| **Muted Slate** | `oklch(0.55 0.02 260)` / `#68707D` | النص الثانوي |
| **Line Whisper** | `oklch(0.9 0.006 250)` / `#E2E7E4` | الفواصل والحدود البنيوية |
| **Growth Emerald** | `oklch(0.585 0.15 165)` / `#0E9F6E` | **المميز الوحيد:** الربح، CTA الأساسي، التقدم |
| **Ember Terracotta** | `oklch(0.58 0.16 32)` / `#C65D3A` | الخطر/النقص فقط (مرتجع، مخزون منخفض) |
| **Amber Hold** | `oklch(0.75 0.13 85)` / `#C99A2B` | الانتظار (قيد التحضير) — نص على سطح فاتح فقط |

- التشبع < 80% دائماً. لا توهجات نيون خارجية. الظلال ملوّنة بلون الخلفية، لا أسود خالص.
- الوضع الداكن: نفس الأدوار، `Canvas` يصبح `oklch(0.2 0.015 260)`، والمميز يُفتّح إلى `oklch(0.72 0.14 165)` مع الحفاظ على تباين AA (4.5:1 للنص).
- قاعدة: زر أساسي واحد مملوء في كل عرض. الثانوية محايدة (outline/ghost).

## 3. Typography Rules

- **Display (أرقام الربح/العناوين):** `IBM Plex Sans Arabic` 600–700، `tracking-tight`، `leading 1.1`، `text-wrap: balance`. أرقام الربح دائماً `tabular-nums` بخط أكبر بدرجتين من النص المحيط.
- **Body:** نفس العائلة 400–500، `leading 1.6`، `max-w-[65ch]`، `text-wrap: pretty` للوصف.
- **Mono (باركود/مرجع/طوابع):** `IBM Plex Mono` للباركود وأرقام الفواتير فقط.
- **المقاسات:** 12 شرح، 14 واجهات/قوائم، 16 نص قراءة (والمدخلات 16px على الموبايل لمنع زوم iOS)، 20–24 عناوين أقسام، 36–48 رقم الفائدة فقط.
- **محظور:** Inter كافتراضي، أي serif (Fraunces/Instrument/Times) في اللوحات، UPPERCASE مخزّن في النص (استخدم `text-transform`)، `leading-none` لنص يلتف 3+ أسطر.
- **عربي/فرنسي:** `lang` و`dir` على الجذر (`ar`→RTL، `fr`→LTR)، خصائص منطقية (`inline-start`) لا `left/right`، أرقام العملة تبقى لاتينية مع `دج` بعد الرقم: `12,500 دج`.

## 4. Component Stylings

- **الأزرار:** نصف قطر 10px موحد، ضغطة `scale(0.96)` خلال 160ms `ease-out`، لا توهج. الأيقونة `currentColor` بوزن يطابق النص (1.5px بجانب 400). نص الزر سطر واحد دائماً (≤3 كلمات).
- **الأيقونات:** عائلة واحدة فقط (`@phosphor-icons/react`)، وزن regular، مقاس 20px في التنقل و16–18px داخل النص. لا إيموجي أبداً.
- **التنبيهات:** `sonner` أسفل-المنتصف، نجاح/خطأ فقط، بلا إزعاج للعمليات عالية التكرار. الأخطاء الميدانية تبقى مضمّنة قرب الحقل.
- **الطباعة:** كل كروم التطبيق (`aside`/`header`/التنقل) يحمل `print:hidden` — تُطبع الفواتير والتقارير نظيفة.
- **البطاقات:** تُستخدم فقط لتسلسل حقيقي (ملخص مالي، وردية). نصف قطر 16px خارجي = 8px داخلي + 8px حشوة (concentric). ظل ناعم ملوّن، لا حدود مزدوجة مع الظل. الكثافة العالية (>7 عناصر) تُستبدل بفواصل `border-t` ومسافة.
- **المدخلات:** اللافتة فوق الحقل، المساعدة اختيارية، الخطأ تحت الحقل + `aria-invalid` + `aria-describedby`. حلقة تركيز `:focus-visible` بعرض 2px تُرى على كل الخلفيات. لا placeholder كلافتة.
- **الشارات:** pill كامل الاستدارة للحالة فقط (جاهز/يحضَّر/منخفض). لا ألوان خام خارج الرموز الدلالية.
- **المحمل/الفارغ/الخطأ:** هيكل عظمي `Skeleton` يطابق الشكل النهائي (لا سبينر عام)، حالة فارغة بتوجيه للإضافة، خطأ مضمّن قرب الحقل، وتنبيهات عابرة عبر `sonner` فقط.
- **الجداول:** أرقام يمين/نهاية السطر بـ `tabular-nums`، صف الرأس ثابت، لا `space-y` — `flex gap`.

## 5. Layout Principles

- حاوية `max-w-[1400px]`، شبكة Grid لا حسابات flex (`grid md:grid-cols-3 gap-4`)، تجميع بالمسافة (الفجوة بين المجموعات ≥ 2× داخلها: 8px داخلية → 16px+ خارجية)، وفواصل فقط عند عجز المسافة.
- عناصر التحكم مميزة عن المحتوى (خلفية/حد/منطقة ثابتة)، وحواف محاذاة مشتركة واحدة، والترتيب بالأهمية من الأعلى وحافة البداية.
- المحتوى ينزف للحواف (خلفيات/صور)، وعناصر التحكم والنص داخل هوامش التخطيط + `safe-area`. أزرار المحتوى لا تلمس الحواف (تبدأ ~16px على الموبايل).
- POS: شبكة منتجات `2–4 أعمدة` + سلة لاصقة، أهداف لمس ≥44px بفجوة 12px للممتلئ و24px حول النصي. المطبخ: أعمدة حالة (جديد/يحضَّر/جاهز) تنهار لعمود واحد <768px.
- كل تخطيط متعدد الأعمدة يصرّح بسقوط <768px في نفس المكوّن. لا عرض ثابت لحاويات نصية (تتحمل نمو الترجمة FR +40%).

## 6. Motion & Interaction

- منحنى واحد: `cubic-bezier(0.23,1,0.32,1)` للدخول/الخروج. مدد: ضغطة 100–160ms، منبثق 125–200ms، قائمة 150–250ms، مودال 200–300ms كحد أقصى. لا `transition: all` (حدد `transform,opacity`)، لا `ease-in` للواجهة، لا دخول من `scale(0)` (ابدأ 0.95+opacity).
- لا حركة إطلاقاً على: أزرار POS، اختصارات لوحة المفاتيح، تنقل القوائم المتكرر. التدرج (stagger) 40ms لمرة واحدة عند دخول اللوحة فقط.
- منبثقات origin-aware (تتمدد من الزناد، والمودال من المركز فقط)، تولتيب يتخطى التأخير بعد أول فتح، وتحولات قابلة للمقاطعة (transitions لا keyframes للواجهة الديناميكية).
- `prefers-reduced-motion: reduce` يقتل الانزلاق/التكبير/التثبيت، ويبقي تلاشي opacity، ويوقف التشغيل التلقائي. كل تغيير متحرك له إشارة ثابتة مرافقة (لون/أيقونة/نص).

## 7. Anti-Patterns (Banned)

لا إيموجي كأيقونات (SVG فقط)، لا Inter افتراضي، لا serif في اللوحات، لا pure black/white، لا توهج خارجي، لا نص متدرج كبير، لا مؤشر مخصص، لا عناصر متداخلة فوق بعضها، لا 3 بطاقات متساوية، لا أسماء وهمية (Acme/John Doe)، لا أرقام مفبركة، لا `LABEL // YEAR`، لا كليشيهات (Seamless/Unleash/Next-Gen)، لا "مرر للأسفل"، لا روابط Unsplash مكسورة (استخدم صوراً حقيقية أو اترك خانة موسومة)، لا CTA مكرر النية، لا زر يلتف لسطرين، لا تباين أزرار فاشل، لا nav بسطرين أو أعلى من 80px.
